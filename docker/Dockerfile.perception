FROM python:3.11-slim

WORKDIR /app

COPY requirements/base.txt .
COPY requirements/perception.txt .
RUN pip install --no-cache-dir -r perception.txt

COPY src/ ./src/

EXPOSE 8001

CMD ["uvicorn", "src.integrations.perception_service:app", "--host", "0.0.0.0", "--port", "8001"]
